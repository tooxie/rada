import { h } from "preact";

import Options from "../../components/options";

import icon from "./settings.svg";

const Settings = () => {
  return <Options icon={icon}>It works Fernando!</Options>;
};

export default Settings;
